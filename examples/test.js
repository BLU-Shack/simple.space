const api = require('../src/spacerun');

const package = new api('none', '463803888072523797', undefined, false);

console.log('Edit the function to test things. If you are trying to get something, append .then(console.log) to the end (remove the ";" at the end)');
package.doSomething();